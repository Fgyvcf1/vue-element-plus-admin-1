# 项目启动说明

## 问题解决

**问题**：居民查询数据总提示500错误
**原因**：后端服务没有启动
**状态**：✅ 已解决

## 启动项目

### 方式1：一键启动（推荐）

**Windows:**
```bash
start-all.bat
```

**Linux/Mac:**
```bash
chmod +x start-all.sh
./start-all.sh
```

### 方式2：手动启动

**1. 启动后端服务:**
```bash
cd backend
node start-backend.js
```

**2. 启动前端服务（新终端窗口）:**
```bash
# 使用 pnpm（必须，根据skill要求）
pnpm dev
```

## 服务地址

- **前端**: http://localhost:5173
- **后端**: http://localhost:3001（固定端口）
- **居民查询API**: http://localhost:3001/api/residents

## 重要提示

1. **必须使用 pnpm**：根据 Vue2To3-Migration-Helper skill 要求
   - ✅ 使用: `pnpm install`, `pnpm add`, `pnpm dev`
   - ❌ 禁止: `npm install`, `yarn install`

2. **数据库要求**: MariaDB 服务必须运行
   - 配置: localhost:3306
   - 数据库: village
   - 用户: app_user

3. **前端后端都要启动**：
   - 前端：提供Vue3界面
   - 后端：提供API接口

## 验证服务

**检查后端服务:**
```bash
curl http://localhost:3001/api/residents
```

应该返回:
```json
{
  "code": 20000,
  "data": [...],
  "total": 213
}
```

**检查前端服务:**
打开浏览器访问: http://localhost:5173

## 常见问题

### 1. 后端服务启动失败

**原因**: 端口3001被占用
**解决**:
```bash
# 后端启动时会自动杀死占用3001端口的进程
# 如果仍有问题，手动查找并终止
netstat -ano | findstr :3001
taskkill /F /PID <进程ID>
```

### 2. 数据库连接失败

**原因**: MariaDB服务未启动
**解决**:
```bash
# Windows: 启动MariaDB服务
net start mysql

# 或手动启动MariaDB服务
```

### 3. 前端无法访问后端

**原因**: CORS配置问题
**解决**: 确认 `start-backend.js` 中的CORS配置包含前端地址

## 模块编辑

当编辑特定模块时，会自动识别并显示：
- 🔍 编辑【居民管理】模块
- 🔍 编辑【调解档案】模块
- 等等

## 开发规范

1. **包管理器**: 只使用 pnpm
2. **图标**: 使用 Element Plus SVG 图标组件
3. **语法**: Vue3 Composition API (`<script setup>`)
4. **状态管理**: Pinia (替代 Vuex)
5. **路由**: Vue Router 4
